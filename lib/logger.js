(function(){
    'use strict';

    module.exports.info = (message) => {
        console.log(message);
    }

    module.exports.debug = (message) => {
        console.log(message);
    }

}).call(this);